{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Vite App",
            "program": "${workspaceFolder}/node_modules/vite/bin/vite.js",
            "args": ["--port", "5173"],
            "preLaunchTask": "Run Vite Dev Server"
        }
    ]
}
